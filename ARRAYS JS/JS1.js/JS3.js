//function obtenerLargoDelArray(array) {
    // Retornar la longitud del arreglo recibido.
    // Tu código:
 // }
  
//  module.exports = obtenerLargoDelArray;

function obtenerLargoDelArray(array) {
    // Retornar la longitud del arreglo recibido.
    return array.length;
  }
  
  // Ejemplo de uso:
  let ejemploArray = [10, 20, 30, 40];
  console.log(obtenerLargoDelArray(ejemploArray)); // Output: 4
  
  // Exportar la función (si estás usando Node.js)
  module.exports = obtenerLargoDelArray;
  